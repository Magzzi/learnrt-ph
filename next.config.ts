import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'developers.google.com', 'img.icons8.com', 'i.postimg.cc'],
  },
};

export default nextConfig;
